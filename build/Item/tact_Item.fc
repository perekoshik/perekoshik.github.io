#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; tact_Item.headers.fc
;;
;; Header files for Item
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_context_get
(int, slice, int, slice) __tact_context_get() inline;

;; __tact_context_get_sender
slice __tact_context_get_sender() inline;

;; $NewOrder$_store
builder $NewOrder$_store(builder build_0, (slice, int, int) v) inline;

;; $NewOrder$_store_cell
cell $NewOrder$_store_cell((slice, int, int) v, builder b) inline;

;; $NewItem$_store
builder $NewItem$_store(builder build_0, tuple v) inline;

;; $NewItem$_store_cell
cell $NewItem$_store_cell(tuple v, builder b) inline;

;; $Item$_store
builder $Item$_store(builder build_0, (slice, int, int, slice, slice, slice, int) v) inline;

;; $Item$_load
(slice, ((slice, int, int, slice, slice, slice, int))) $Item$_load(slice sc_0) inline;

;; $Context$_get_value
_ $Context$_get_value((int, slice, int, slice) v) inline;

;; $Order$init$_store
builder $Order$init$_store(builder build_0, (slice, slice, slice, int, slice) v) inline;

;; $Item$init$_load
(slice, ((slice, int))) $Item$init$_load(slice sc_0) inline;

;; $Item$_contract_init
(slice, int, int, slice, slice, slice, int) $Item$_contract_init(slice $shop, int $id) impure inline;

;; $Item$_contract_load
(slice, int, int, slice, slice, slice, int) $Item$_contract_load() impure inline;

;; $Item$_contract_store
() $Item$_contract_store((slice, int, int, slice, slice, slice, int) v) impure inline;

;; $Cell$_fun_asSlice
slice $Cell$_fun_asSlice(cell $self) impure inline;

;; $global_newAddress
slice $global_newAddress(int $chain, int $hash) impure inline;

;; $global_contractAddressExt
slice $global_contractAddressExt(int $chain, cell $code, cell $data) impure inline;

;; $global_contractAddress
slice $global_contractAddress((cell, cell) $s) impure inline;

;; $MessageParameters$_constructor_bounce_to_value_mode_body
((int, cell, int, slice, int)) $MessageParameters$_constructor_bounce_to_value_mode_body(int $bounce, slice $to, int $value, int $mode, cell $body) inline;

;; $Order$_init_child
(cell, cell) $Order$_init_child(slice $seller, slice $buyer, slice $itemAddress, int $id, slice $deliveryAddress) inline_ref;

;; $Item$_fun_shop
((slice, int, int, slice, slice, slice, int), slice) $Item$_fun_shop((slice, int, int, slice, slice, slice, int) $self) impure inline_ref;

;; $Item$_fun_id
((slice, int, int, slice, slice, slice, int), int) $Item$_fun_id((slice, int, int, slice, slice, slice, int) $self) impure inline_ref;

;; $Item$_fun_price
((slice, int, int, slice, slice, slice, int), int) $Item$_fun_price((slice, int, int, slice, slice, slice, int) $self) impure inline_ref;

;; $Item$_fun_imageSrc
((slice, int, int, slice, slice, slice, int), slice) $Item$_fun_imageSrc((slice, int, int, slice, slice, slice, int) $self) impure inline_ref;

;; $Item$_fun_title
((slice, int, int, slice, slice, slice, int), slice) $Item$_fun_title((slice, int, int, slice, slice, slice, int) $self) impure inline_ref;

;; $Item$_fun_description
((slice, int, int, slice, slice, slice, int), slice) $Item$_fun_description((slice, int, int, slice, slice, slice, int) $self) impure inline_ref;

;; $Item$_fun_available
((slice, int, int, slice, slice, slice, int), int) $Item$_fun_available((slice, int, int, slice, slice, slice, int) $self) impure inline_ref;

;; $Item$_fun_reply
((slice, int, int, slice, slice, slice, int), ()) $Item$_fun_reply((slice, int, int, slice, slice, slice, int) $self, cell $body) impure inline;

;; $SendParameters$_constructor_to_value_mode_body
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_body(slice $to, int $value, int $mode, cell $body) inline;

;; $NewItem$_constructor_
(tuple) $NewItem$_constructor_() inline;

;; $SendParameters$_constructor_to_value_mode_code_data
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_code_data(slice $to, int $value, int $mode, cell $code, cell $data) inline;

;; $SendParameters$_constructor_to_value_body_mode
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_body_mode(slice $to, int $value, cell $body, int $mode) inline;

;; $NewOrder$_constructor_deliveryAddress_itemIndex_price
((slice, int, int)) $NewOrder$_constructor_deliveryAddress_itemIndex_price(slice $deliveryAddress, int $itemIndex, int $price) inline;


;; tact_Item.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

(int, slice, int, slice) __tact_context_get() inline {
    return __tact_context;
}

slice __tact_context_get_sender() inline {
    return __tact_context_sender;
}

builder $global_beginCell() impure asm """
    NEWC
""";

int $global_contractHash(cell $code, cell $data) impure asm """
    s0 PUSH HASHCU // `data` hash
    s2 PUSH HASHCU // `code` hash
    SWAP2
    CDEPTH         // `data` depth
    SWAP
    CDEPTH         // `code` depth
    131380 INT     // (2 << 16) | (1 << 8) | 0x34

    // Group 2: Composition of the Builder
    NEWC
    24 STU  // store refs_descriptor | bits_descriptor | data
    16 STU  // store depth_descriptor for `code`
    16 STU  // store depth_descriptor for `data`
    256 STU // store `code` hash
    256 STU // store `data` hash

    // Group 3: SHA256 hash of the resulting Builder
    ONE HASHEXT_SHA256
""";

slice $Slice$_fun_asAddressUnsafe(slice $self) impure asm "NOP";

slice $Cell$_fun_beginParse(cell $self) impure asm """
    CTOS
""";

slice $Cell$_fun_asSlice(cell $self) impure inline {
    var ($self) = $self;
    return $Cell$_fun_beginParse($self);
}

cell $Builder$_fun_endCell(builder $self) impure asm """
    ENDC
""";

slice $global_newAddress(int $chain, int $hash) impure inline {
    return $Slice$_fun_asAddressUnsafe($Cell$_fun_asSlice($Builder$_fun_endCell(store_uint(store_int(store_uint($global_beginCell(), 4, 3), $chain, 8), $hash, 256))));
}

slice $global_contractAddressExt(int $chain, cell $code, cell $data) impure inline {
    int $hash = $global_contractHash($code, $data);
    return $global_newAddress($chain, $hash);
}

slice $global_contractAddress((cell, cell) $s) impure inline {
    var (($s'code, $s'data)) = $s;
    return $global_contractAddressExt(0, $s'code, $s'data);
}

slice $global_myAddress() impure asm """
    MYADDR
""";

int $global_myBalance() impure asm """
    BALANCE FIRST
""";

() $global_nativeReserve(int $amount, int $mode) impure asm """
    RAWRESERVE
""";

() $global_message((int, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    106 PUSHINT         // 1 + 4 + 4 + 64 + 32 + 1
    STZEROES
    // → Stack state
    // s0: Builder
    // s1: `body`
    // s2: `mode`
    STDICT
    ENDC
    SWAP
    SENDRAWMSG
""";

() $global_send((int, cell, cell, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    105 PUSHINT         // 1 + 4 + 4 + 64 + 32
    STZEROES            // store currency_collection, ihr_fee, fwd_fee, created_lt and created_at
    // → Stack state
    // s0: Builder
    // s1: `data`
    // s2: `code`
    // s3: `body`
    // s4: `mode`

    // Group 2: Placing the Builder after code and data, then checking those for nullability
    s2 XCHG0
    DUP2
    ISNULL
    SWAP
    ISNULL
    AND
    // → Stack state
    // s0: -1 (true) if `data` and `code` are both null, 0 (false) otherwise
    // s1: `code`
    // s2: `data`
    // s3: Builder
    // s4: `body`
    // s5: `mode`

    // Group 3: Left branch of the IFELSE, executed if s0 is -1 (true)
    <{
        DROP2 // drop `data` and `code`, since either of those is null
        b{0} STSLICECONST
    }> PUSHCONT

    // Group 3: Right branch of the IFELSE, executed if s0 is 0 (false)
    <{
        // _ split_depth:(Maybe (## 5))
        //   special:(Maybe TickTock)
        //   code:(Maybe ^Cell)
        //   data:(Maybe ^Cell)
        //   library:(Maybe ^Cell)
        // = StateInit;
        ROT                // place message Builder on top
        b{10} STSLICECONST // store Maybe = true, Either = false
        // Start composing inlined StateInit
        b{00} STSLICECONST // store split_depth and special first
        STDICT             // store code
        STDICT             // store data
        b{0} STSLICECONST  // store library
    }> PUSHCONT

    // Group 3: IFELSE that does the branching shown above
    IFELSE
    // → Stack state
    // s0: Builder
    // s1: null or StateInit
    // s2: `body`
    // s3: `mode`

    // Group 4: Finalizing the message
    STDICT // store `body` as ref with an extra Maybe bit, since `body` might be null
    ENDC
    // → Stack state
    // s0: Cell
    // s1: `mode`

    // Group 5: Sending the message, with `mode` on top
    SWAP
    SENDRAWMSG // https://github.com/tact-lang/tact/issues/1558
""";

((slice, int, int, slice, slice, slice, int), ()) $Item$_fun_reply((slice, int, int, slice, slice, slice, int) $self, cell $body) impure inline {
    var (($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available)) = $self;
    slice $to = __tact_context_get_sender();
    int $bounce = true;
    if (false) {
        int $balance = $global_myBalance();
        int $balanceBeforeMessage = ($balance - $Context$_get_value(__tact_context_get()));
        if (($balanceBeforeMessage < 0)) {
            $global_nativeReserve(0, 0);
            $global_message($MessageParameters$_constructor_bounce_to_value_mode_body($bounce, $to, 0, 130, $body));
            return (($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available), ());
        }
    }
    $global_message($MessageParameters$_constructor_bounce_to_value_mode_body($bounce, $to, 0, 66, $body));
    return (($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available), ());
}

;; tact_Item.constants.fc
;; String "imageSrc"
slice __gen_slice_string_65a388760d613a486e9222653676e93876e62a41886a69e2f9451728510c62ed() asm """
    B{b5ee9c7241010101000a000010696d616765537263975074ea} B>boc <s PUSHSLICE
""";

;; String "title"
slice __gen_slice_string_effb68c8cd138379ea31a4b10b902e0413a59126eaeb9564f1ed82ef56355500() asm """
    B{b5ee9c7241010101000700000a7469746c65ddf1d456} B>boc <s PUSHSLICE
""";

;; String "description"
slice __gen_slice_string_140bdd99e205d5912ca97650fb28f063726b262214aacff74252f2bcee780c53() asm """
    B{b5ee9c7241010101000d0000166465736372697074696f6ee0124c71} B>boc <s PUSHSLICE
""";

;; Cell WHOp5/r7z1OkoDvgCS6ZiQ27q3uFmO/Vj/PxNQc6K90=
cell __gen_cell_cell_5873a9e7fafbcf53a4a03be0092e99890dbbab7b8598efd58ff3f135073a2bdd() asm """
    B{b5ee9c72410101010013000022000000004f726465722063726561746564fbf3bdfe} B>boc PUSHREF
""";

;; tact_Item.storage.fc
;;
;; Type: Context
;; TLB: _ bounceable:bool sender:address value:int257 raw:^slice = Context
;;

_ $Context$_get_value((int, slice, int, slice) v) inline {
    var (v'bounceable, v'sender, v'value, v'raw) = v;
    return v'value;
}

;;
;; Type: SendParameters
;; TLB: _ mode:int257 body:Maybe ^cell code:Maybe ^cell data:Maybe ^cell value:int257 to:address bounce:bool = SendParameters
;;

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_body(slice $to, int $value, int $mode, cell $body) inline {
    return ($mode, $body, null(), null(), $value, $to, true);
}

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_code_data(slice $to, int $value, int $mode, cell $code, cell $data) inline {
    return ($mode, null(), $code, $data, $value, $to, true);
}

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_body_mode(slice $to, int $value, cell $body, int $mode) inline {
    return ($mode, $body, null(), null(), $value, $to, true);
}

;;
;; Type: MessageParameters
;; TLB: _ mode:int257 body:Maybe ^cell value:int257 to:address bounce:bool = MessageParameters
;;

((int, cell, int, slice, int)) $MessageParameters$_constructor_bounce_to_value_mode_body(int $bounce, slice $to, int $value, int $mode, cell $body) inline {
    return ($mode, $body, $value, $to, $bounce);
}

;;
;; Type: NewOrder
;; Header: 0x90268e31
;; TLB: new_order#90268e31 deliveryAddress:^string itemIndex:uint256 price:coins = NewOrder
;;

builder $NewOrder$_store(builder build_0, (slice, int, int) v) inline {
    var (v'deliveryAddress, v'itemIndex, v'price) = v;
    build_0 = store_uint(build_0, 2418445873, 32);
    build_0 = build_0.store_builder_ref(begin_cell().store_slice(v'deliveryAddress));
    build_0 = build_0.store_uint(v'itemIndex, 256);
    build_0 = build_0.store_varuint16(v'price);
    return build_0;
}

cell $NewOrder$_store_cell((slice, int, int) v, builder b) inline {
    return $NewOrder$_store(b, v).end_cell();
}

((slice, int, int)) $NewOrder$_constructor_deliveryAddress_itemIndex_price(slice $deliveryAddress, int $itemIndex, int $price) inline {
    return ($deliveryAddress, $itemIndex, $price);
}

;;
;; Type: NewItem
;; Header: 0xd6e1cf22
;; TLB: new_item#d6e1cf22  = NewItem
;;

builder $NewItem$_store(builder build_0, tuple v) inline {
    build_0 = store_uint(build_0, 3605122850, 32);
    return build_0;
}

cell $NewItem$_store_cell(tuple v, builder b) inline {
    return $NewItem$_store(b, v).end_cell();
}

(tuple) $NewItem$_constructor_() inline {
    return empty_tuple();
}

;;
;; Type: Order
;; TLB: _ seller:address buyer:address itemAddress:address id:uint256 completed:bool deliveryAddress:^string = Order
;;

builder $Order$init$_store(builder build_0, (slice, slice, slice, int, slice) v) inline {
    var (v'seller, v'buyer, v'itemAddress, v'id, v'deliveryAddress) = v;
    build_0 = build_0.store_slice(v'seller);
    build_0 = build_0.store_slice(v'buyer);
    build_0 = build_0.store_slice(v'itemAddress);
    var build_1 = begin_cell();
    build_1 = build_1.store_uint(v'id, 256);
    build_1 = build_1.store_builder_ref(begin_cell().store_slice(v'deliveryAddress));
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

cell $Order$_child_get_code() impure asm """
    B{} B>boc PUSHREF
""";

(cell, cell) $Order$_init_child(slice $seller, slice $buyer, slice $itemAddress, int $id, slice $deliveryAddress) inline_ref {
    ;; Build init code cell

    ;; Contract Code: Order
    cell init_code = $Order$_child_get_code();


    builder b = begin_cell();
    b = b.store_int(false, 1);
    b = $Order$init$_store(b, ($seller, $buyer, $itemAddress, $id, $deliveryAddress));
    return (init_code, b.end_cell());
}

;;
;; Type: Item
;; TLB: _ shop:address id:uint256 price:coins imageSrc:^string title:^string description:^string available:bool = Item
;;

builder $Item$_store(builder build_0, (slice, int, int, slice, slice, slice, int) v) inline {
    var (v'shop, v'id, v'price, v'imageSrc, v'title, v'description, v'available) = v;
    build_0 = build_0.store_slice(v'shop);
    build_0 = build_0.store_uint(v'id, 256);
    build_0 = build_0.store_varuint16(v'price);
    build_0 = build_0.store_builder_ref(begin_cell().store_slice(v'imageSrc));
    var build_1 = begin_cell();
    build_1 = build_1.store_builder_ref(begin_cell().store_slice(v'title));
    build_1 = build_1.store_builder_ref(begin_cell().store_slice(v'description));
    build_1 = build_1.store_int(v'available, 1);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

(slice, ((slice, int, int, slice, slice, slice, int))) $Item$_load(slice sc_0) inline {
    var v'shop = sc_0~load_msg_addr();
    var v'id = sc_0~load_uint(256);
    var v'price = sc_0~load_varuint16();
    var v'imageSrc = sc_0~load_ref().begin_parse();
    slice sc_1 = sc_0~load_ref().begin_parse();
    var v'title = sc_1~load_ref().begin_parse();
    var v'description = sc_1~load_ref().begin_parse();
    var v'available = sc_1~load_int(1);
    return (sc_0, (v'shop, v'id, v'price, v'imageSrc, v'title, v'description, v'available));
}

(slice, ((slice, int))) $Item$init$_load(slice sc_0) inline {
    var v'shop = sc_0~load_msg_addr();
    var v'id = sc_0~load_uint(256);
    return (sc_0, (v'shop, v'id));
}

(slice, int, int, slice, slice, slice, int) $Item$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$Item$_load();
    }
    else {
        (slice $shop, int $id) = $sc~$Item$init$_load();
        $sc.end_parse();
        return $Item$_contract_init($shop, $id);
    }
}

() $Item$_contract_store((slice, int, int, slice, slice, slice, int) v) impure inline {
    builder b = begin_cell();
    b = b.store_int(true, 1);
    b = $Item$_store(b, v);
    set_data(b.end_cell());
}

;;
;; Contract Item functions
;;

(slice, int, int, slice, slice, slice, int) $Item$_contract_init(slice $shop, int $id) impure inline {
    var (($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available)) = (null(), null(), null(), null(), null(), null(), null());
    $self'shop = $shop;
    $self'id = $id;
    $self'price = 0;
    $self'imageSrc = __gen_slice_string_65a388760d613a486e9222653676e93876e62a41886a69e2f9451728510c62ed();
    $self'title = __gen_slice_string_effb68c8cd138379ea31a4b10b902e0413a59126eaeb9564f1ed82ef56355500();
    $self'description = __gen_slice_string_140bdd99e205d5912ca97650fb28f063726b262214aacff74252f2bcee780c53();
    $self'available = true;
    return ($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available);
}

((slice, int, int, slice, slice, slice, int), slice) $Item$_fun_shop((slice, int, int, slice, slice, slice, int) $self) impure inline_ref {
    var (($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available)) = $self;
    var $fresh$ret_61 = $self'shop;
    return (($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available), $fresh$ret_61);
}

((slice, int, int, slice, slice, slice, int), int) $Item$_fun_id((slice, int, int, slice, slice, slice, int) $self) impure inline_ref {
    var (($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available)) = $self;
    var $fresh$ret_62 = $self'id;
    return (($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available), $fresh$ret_62);
}

((slice, int, int, slice, slice, slice, int), int) $Item$_fun_price((slice, int, int, slice, slice, slice, int) $self) impure inline_ref {
    var (($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available)) = $self;
    var $fresh$ret_63 = $self'price;
    return (($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available), $fresh$ret_63);
}

((slice, int, int, slice, slice, slice, int), slice) $Item$_fun_imageSrc((slice, int, int, slice, slice, slice, int) $self) impure inline_ref {
    var (($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available)) = $self;
    var $fresh$ret_64 = $self'imageSrc;
    return (($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available), $fresh$ret_64);
}

((slice, int, int, slice, slice, slice, int), slice) $Item$_fun_title((slice, int, int, slice, slice, slice, int) $self) impure inline_ref {
    var (($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available)) = $self;
    var $fresh$ret_65 = $self'title;
    return (($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available), $fresh$ret_65);
}

((slice, int, int, slice, slice, slice, int), slice) $Item$_fun_description((slice, int, int, slice, slice, slice, int) $self) impure inline_ref {
    var (($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available)) = $self;
    var $fresh$ret_66 = $self'description;
    return (($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available), $fresh$ret_66);
}

((slice, int, int, slice, slice, slice, int), int) $Item$_fun_available((slice, int, int, slice, slice, slice, int) $self) impure inline_ref {
    var (($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available)) = $self;
    var $fresh$ret_67 = $self'available;
    return (($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available), $fresh$ret_67);
}

;;
;; Get methods of a Contract Item
;;

_ %shop() method_id(102112) {
    var self = $Item$_contract_load();
    var res = self~$Item$_fun_shop();
    return res;
}

_ %id() method_id(105872) {
    var self = $Item$_contract_load();
    var res = self~$Item$_fun_id();
    return res;
}

_ %price() method_id(120091) {
    var self = $Item$_contract_load();
    var res = self~$Item$_fun_price();
    return res;
}

_ %imageSrc() method_id(80358) {
    var self = $Item$_contract_load();
    var res = self~$Item$_fun_imageSrc();
    return res;
}

_ %title() method_id(84137) {
    var self = $Item$_contract_load();
    var res = self~$Item$_fun_title();
    return res;
}

_ %description() method_id(65569) {
    var self = $Item$_contract_load();
    var res = self~$Item$_fun_description();
    return res;
}

_ %available() method_id(66974) {
    var self = $Item$_contract_load();
    var res = self~$Item$_fun_available();
    return res;
}

;;
;; Routing of a Contract Item
;;

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_internal(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available) = $Item$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) { return (); }
    var op = in_msg~load_opcode_internal();
    ;; Receive UpdateItem message
    if (op == 967082613) {
        var $msg'price = in_msg~load_varuint16();
        var $msg'imageSrc = in_msg~load_ref().begin_parse();
        var $msg'title = in_msg~load_ref().begin_parse();
        var $msg'description = in_msg~load_ref().begin_parse();
        $self'price = $msg'price;
        $self'imageSrc = $msg'imageSrc;
        $self'title = $msg'title;
        $self'description = $msg'description;
        $global_send($SendParameters$_constructor_to_value_mode_body($self'shop, 5000000, 2, $NewItem$_store_cell($NewItem$_constructor_(), begin_cell())));
        $Item$_contract_store(($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available));
        return ();
    }
    
    ;; Receive CreateOrder message
    if (op == 0x7ac14988) {
        var $msg'orderId = in_msg~load_int(257);
        var $msg'itemAddress = in_msg~load_msg_addr();
        var $msg'price = in_msg~load_varuint16();
        var $msg'deliveryAddress = in_msg~load_ref().begin_parse();
        var ($init'code, $init'data) = $Order$_init_child($self'shop, __tact_context_get_sender(), $global_myAddress(), $msg'orderId, $msg'deliveryAddress);
        $global_send($SendParameters$_constructor_to_value_mode_code_data($global_contractAddress(($init'code, $init'data)), $msg'price, 2, $init'code, $init'data));
        $global_send($SendParameters$_constructor_to_value_body_mode($self'shop, $msg'price, $NewOrder$_store_cell($NewOrder$_constructor_deliveryAddress_itemIndex_price($msg'deliveryAddress, $self'id, $self'price), begin_cell()), 2));
        ($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available)~$Item$_fun_reply(__gen_cell_cell_5873a9e7fafbcf53a4a03be0092e99890dbbab7b8598efd58ff3f135073a2bdd());
        $Item$_contract_store(($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available));
        return ();
    }
    
    ;; Receive ChangeAvailability message
    if (op == 1224645492) {
        var $msg = empty_tuple();
        $self'available = (~ $self'available);
        $Item$_contract_store(($self'shop, $self'id, $self'price, $self'imageSrc, $self'title, $self'description, $self'available));
        return ();
    }
    
    ;; Throw if not handled
    throw(130);
}


() __tact_selector_hack_asm() impure asm """
@atend @ 1 {
        execute current@ context@ current!
        {
            // The core idea of this function is to save gas by avoiding unnecessary dict jump, when recv_internal/recv_external is called
            // We want to extract recv_internal/recv_external from the dict and select needed function
            // not by jumping to the needed function by it's index, but by using usual IF statements.

            }END> b> // Close previous builder, now we have a cell of previous code on top of the stack

            <{ // Start of the new code builder
                SETCP0
                // Swap the new code builder with the previous code, now we have previous code on top of the stack
                swap
                // Transform cell to slice and load first ref from the previous code, now we have the dict on top of the stack
                <s ref@

                // Extract the recv_internal from the dict
                dup 0 swap @procdictkeylen idict@ { "internal shortcut error" abort } ifnot
                swap

                // Delete the recv_internal from the dict
                0 swap @procdictkeylen idict- drop
                // Delete the recv_external from the dict (it's okay if it's not there)
                -1 swap @procdictkeylen idict- drop
                // Delete the __tact_selector_hack from the dict
                65535 swap @procdictkeylen idict- drop

                // Bring the code builder from the bottom of the stack
                // because if recv_external extraction is optional, and the number of elements on the stack is not fixed
                depth 1- roll
                // Swap with the dict from which we extracted recv_internal and (maybe) recv_external
                swap

                // Check if the dict is empty
                dup null?
                // Store a copy of this flag in the bottom of the stack
                dup depth 1- -roll
                {
                    // If the dict is empty, just drop it (it will be null if it's empty)
                    drop
                }
                {
                    // If the dict is not empty, prepare continuation to be stored in c3
                    <{
                        // Save this dict as first ref in this continuation, it will be pushed in runtime by DICTPUSHCONST
                        swap @procdictkeylen DICTPUSHCONST
                        // Jump to the needed function by it's index
                        DICTIGETJMPZ
                        // If such key is not found, throw 11 along with the key as an argument
                        11 THROWARG
                    }> PUSHCONT
                    // Store the continuation in c3
                    c3 POP
                } cond

                // Function id is on top of the (runtime) stack
                DUP IFNOTJMP:<{
                    // place recv_internal here
                    DROP swap @addop
                }>

                // Bring back the flag, indicating if the dict is empty or not from the bottom of the stack
                depth 1- roll
                {
                    // If the dict is empty, throw 11
                    11 THROWARG
                }
                {
                    // If the dict is not empty, jump to continuation from c3
                    c3 PUSH JMPX
                } cond
            }> b>
        } : }END>c
        current@ context! current!
    } does @atend !
""";

() __tact_selector_hack() method_id(65535) {
    return __tact_selector_hack_asm();
}